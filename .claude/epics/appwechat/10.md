---
name: Database Schema & Models
status: open
created: 2025-09-10T09:16:37Z
updated: 2025-09-10T13:04:43Z
github: https://github.com/aixiaojiao/appwechat/issues/10
depends_on: [8]
parallel: false
conflicts_with: []
---

# Task 002: Database Schema & Models

## Description

Design and implement the complete database schema for the StorySpark appwechat project. This includes creating users and devices tables with proper relationships, implementing corresponding data models with validation, setting up database migrations for schema versioning, and configuring connection pooling for optimal performance.

## Acceptance Criteria

- Users table created with all required fields and proper constraints
- Devices table created with foreign key relationships to users
- Data models implement proper validation and business rules
- Database migrations are versioned and can be run/rolled back safely
- Connection pooling is configured for optimal performance
- All database operations use prepared statements for security

## Technical Details

### Database Schema

#### Users Table
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  wechat_openid VARCHAR(255) UNIQUE NOT NULL,
  nickname VARCHAR(100),
  avatar_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  last_login TIMESTAMP WITH TIME ZONE
);
```

#### Devices Table
```sql
CREATE TABLE devices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  device_id VARCHAR(255) NOT NULL,
  device_type VARCHAR(50),
  os_version VARCHAR(50),
  app_version VARCHAR(50),
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(user_id, device_id)
);
```

### Data Models
- Implement User and Device models with proper validation
- Add associations and relationship methods
- Include data sanitization and business logic
- Implement soft delete functionality where appropriate

### Migration System
- Set up migration framework for schema versioning
- Create initial migration files
- Implement rollback capabilities
- Add migration status tracking

## Dependencies

- **001**: Backend Foundation Setup (requires database connection)

## Effort Estimate

**Size: S (Small)**
**Estimated Hours: 8**

Breakdown:
- Database schema design: 2 hours
- Model implementation: 3 hours
- Migration system setup: 2 hours
- Testing and validation: 1 hour

## Definition of Done

- [ ] Users table created with all required fields and indexes
- [ ] Devices table created with proper foreign key relationships
- [ ] User model implements validation and business rules
- [ ] Device model implements validation and associations
- [ ] Migration system is functional and documented
- [ ] All models have corresponding tests
- [ ] Connection pooling is configured and tested
- [ ] Database constraints prevent invalid data
- [ ] All database operations use prepared statements